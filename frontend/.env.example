# Frontend environment variables
# URL used by Vite dev-server proxy to forward /api requests to the backend.
# Change to http://backend:3001 when running inside Docker Compose.
VITE_BACKEND_URL=http://localhost:3001
